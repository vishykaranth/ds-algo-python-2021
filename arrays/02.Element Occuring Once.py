# Given and array where every element occurs three times, except one element that occurs only once.
# Find the element that occurs once
# Bit Manipulation

def elementOccuringOnce(elements):
    return 0


print(elementOccuringOnce([12, 1, 12, 3, 12, 1, 1, 2, 3, 3]))
